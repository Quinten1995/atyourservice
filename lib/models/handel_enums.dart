enum DealType { s0, s1 }

enum ProvisionType { percent, fixed } // % oder fixer Betrag

enum ProvisionDue { award, handover, finalInvoice }

// fällig bei Zuschlag / bei Übergabe / bei Schlussrechnung
